import { Text } from 'ui';
import { RenderedResults } from '../../components/RenderedResults'

# Text
*Interlock text component*

🔗 [text](#text) ▪️ [variant](#variant) ▪️ [align](#align)

---
## Props
```jsx
interface TextProps {
  /**
   * @param  text display text (Required)
   */
  text: string;
  /**
   * @param CSS overrides provided in-line. (Optional)
   */
  css?: React.CSSProperties;
  /**
   * @param Variant specifies text variant.
   * Options are header, subheader, body or caption
   */
  variant?: "header" | "subheader" | "body" | "caption";
  /**
   * @param align specifies how the text should be aligned
   */
  align?: "left" | "center" | "right";
}
```
---
<div id="text">
### text
  ```jsx
  <>
    <Text text="This is a test" />
  </>
  ```
  <RenderedResults>
    <Text text="This is a test" />
  </RenderedResults>
</div>

---
<div id="variant">
  ### variant
  Description: Specifies the design variant used when rendering text.
  There are four options: *header, subheader, body and caption*
  ```jsx
  <>
    <Text text="Header text" variant="header" />
    <Text text="Subheader text" variant="subheader" />
    <Text text="Body text" variant="body" />
    <Text text="Caption text" variant="caption" />
  </>
  ```
  <RenderedResults>
    <Text text="Header text" variant="header" />
    <Text text="Subheader text" variant="subheader" />
    <Text text="Body text" variant="body" />
    <Text text="Caption text" variant="caption" />
  </RenderedResults>
</div>

---
<div id="align">
  ### align
  Description: Sets underlying text-align property in css.
  ```jsx
  <>
    <Text text="Left aligned" align="left" />
    <Text text="Center aligned" align="center" />
    <Text text="Right aligned" align="right" />
  </>
  ```
  <RenderedResults>
    <Text text="Left aligned" align="left" />
    <Text text="Center aligned" align="center" />
    <Text text="Right aligned" align="right" />
  </RenderedResults>
</div>
