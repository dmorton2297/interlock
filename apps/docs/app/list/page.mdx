import { List } from "ui";
import { RenderedResults } from "../../components/RenderedResults";
import React, { useState } from "react";
import { Box, Modal } from "ui";

export function ModalDemo() {
  const [open, setOpen] = useState(false);

  const handleCloseModal = () => {
    setOpen(false);
  };

  return (
    <Box>
      <Modal
        handleCloseModal={handleCloseModal}
        open={open}
        css={{ width: 300, height: 300 }}
      />
      <button onClick={() => setOpen(true)}>Click me to open modal</button>
    </Box>
  );
}


# List

_Interlock list component_ <br />

This component inherits all params of the Box component<br />

#### List props

🔗 [items](#items) ▪️ [listItemBoxProps](#listItemBoxProps) ▪️ [variant](#variant)

<div id="inherited">
  #### List inherited props Box 
  🔗 [padding](/box#padding) ▪️ [margin](/box#margin) ▪️ [css](/box/#css)
  [className](/box/#css)
  <br />
</div>

## Props

```typescript
interface ListItemProps {
  /**
   * @param key identifies a particular list item
   */
  key: string | number;
  /**
   * @param children content that is rendered fo an item
   */
  children: string | React.ReactNode | React.ReactNode[];
}

interface ListProps extends BoxProps {
  /**
   * @param items Items to display in the list
   */
  items?: ListItemProps[];
  /**
   * @param listItemBoxProps Boxprops that will be applied to each list items container.
   */
  listItemBoxProps?: BoxProps;
  /**
   * @param variant specifies design vaiant
   */
  variant?: "markers" | "borders" | "noformat";
}
```
<div id="items">
### items
  ```jsx
const items = [{ key: 'test_item1', children: <span>Test item 1</span>},
                { key: 'test_item2', children: <span>Test item 2</span>},
                { key: 'test_item1', children: <span>Test item 3</span>}]    
  ```
  ```jsx
  <>
    <List items={items} />
  </>
  ```
  <RenderedResults>
      <List items={[{ key: 'test_item1', children: <span>Test item 1</span> },
                    { key: 'test_item2', children: <span>Test item 2</span> },
                    { key: 'test_item3', children: <span>Test item 3</span> }]} />
  </RenderedResults>
</div>
---
<div id="listItemBoxProps">
### listItemBoxProps
  ```jsx
  <>
    <List items={items} listItemBoxProps={{ margin: 'small' }} />
  </>
  ```
  <RenderedResults>
      <List
        listItemBoxProps={{ padding: 'small' }}
        items={[{ key: 'test_item', children: <span>Example of</span>},
                { key: 'test_item', children: <span>customizing list item</span>},
                { key: 'test_item', children: <span>props</span>}]} />
  </RenderedResults>
</div>
---
<div id="variant">
### variant
 ```jsx
  <>
    <List items={items} variant="markers" />
    <List items={items} variant="borders" />
    <List items={items} variant="noformat" />
  </>
  ```
  <RenderedResults>
    <List
      css={{ padding: 0 }} 
      items={[{ key: 'test_item1', children: <span>Test item 1</span> },
                      { key: 'test_item2', children: <span>Test item 2</span> },
                      { key: 'test_item3', children: <span>Test item 3</span> }]} variant="markers" />
    <List
      css={{ padding: 0 }} 
      items={[{ key: 'test_item1', children: <span>Test item 1</span> },
                    { key: 'test_item2', children: <span>Test item 2</span> },
                    { key: 'test_item3', children: <span>Test item 3</span> }]} variant="borders" />
    <List
      css={{ padding: 0 }}  
      items={[{ key: 'test_item1', children: <span>Test item 1</span> },
                    { key: 'test_item2', children: <span>Test item 2</span> },
                    { key: 'test_item3', children: <span>Test item 3</span> }]} variant="noformat" />
  </RenderedResults>
</div>
